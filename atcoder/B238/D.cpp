#include <bits/stdc++.h>

using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(NULL);

    int t, a, s;
    cin >> t;

    while (t--) {
        cin >> a >> s;
    }


    return 0;
}