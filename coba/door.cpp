#include <bits/stdc++.h>
using namespace std;

int main() {
    int n, h, p[100], v[100];
    cin >> n >> h;
    for (int i=0; i < n; i++) {
        cin >> p[i] >> v[i];
    }

    while(h > 0) {
        
    }




}